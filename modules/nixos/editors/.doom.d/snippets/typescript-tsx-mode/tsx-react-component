# -*- mode: snippet -*-
# name: TSX React component
# key: tsx-react-component
# --
import * as React from 'react';

interface ${1:`(+yas/filename)`}Props {

}

const $1 :React.FC<$1Props> = (props) => (
$0
);

export default $1;
