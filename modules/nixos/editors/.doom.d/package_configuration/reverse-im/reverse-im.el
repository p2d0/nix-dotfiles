;;; package_configuration/reverse-im/reverse-im.el -*- lexical-binding: t; -*-

(use-package! reverse-im
	:disabled t
	;; :ensure t
  ;; :custom
  ;; (reverse-im-input-methods '("russian-computer"))
  ;; ;; (reverse-im-activate "ukrainian-computer") ; the legacy way
  ;; :config
  ;; (reverse-im-mode t)
	)
