;;; editors/.doom.d/package_configuration/tabnine/tabnine.el -*- lexical-binding: t; -*-

(after! company
  ;; (setq +lsp-company-backends '(:separate  company-capf company-yasnippet company-tabnine))
  ;; (setq company-show-quick-access t)
  ;; (setq company-idle-delay 0)
  ;; (setq company-backends '(company-capf (:separate  company-yasnippet company-tabnine)))
  ;; (add-to-list 'company-backends 'company-tabnine)
  )
